(()=>{const q=s=>document.querySelector(s);const header=q('.topbar');const picker=q('.rhythm-picker');const sequencer=q('.sequencer-panel');const learning=q('.learning-hub');const library=q('.presets-panel');const fillLink=q('.feature-page-link');if(fillLink){const kicker=fillLink.querySelector('.section-kicker');const title=fillLink.querySelector('h2');const text=fillLink.querySelector('p');const action=fillLink.querySelector('strong:last-child');if(kicker)kicker.textContent='DRUM FILL VIEWER';if(title)title.textContent='おかずを見て・聴いて学ぶ';if(text)text.textContent='ジャンルとおかずを選んで、配置確認・試聴・シーケンサー編集ができるよ。';if(action)action.textContent='おかず視聴 →'}if(!header||!picker||!sequencer||!learning||!library)return;picker.id='rhythmSection';sequencer.id='sequencerSection';learning.id='learningSection';library.id='librarySection';[picker,sequencer,learning,library].forEach(x=>x.classList.add('panel-anchor'));const nav=document.createElement('nav');nav.className='section-nav';nav.setAttribute('aria-label','セクション移動');[['rhythmSection','リズム'],['sequencerSection','打ち込み'],['learningSection','学習'],['librarySection','一覧']].forEach(([id,label])=>{const b=document.createElement('button');b.type='button';b.textContent=label;b.addEventListener('click',()=>document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'}));nav.append(b)});header.insertAdjacentElement('afterend',nav);const advanced=q('.advanced-panel');if(advanced&&window.matchMedia('(max-width:820px)').matches)advanced.open=false;const grid=q('.learning-grid');if(grid){const cards=[...grid.querySelectorAll('.learning-card')];const switcher=document.createElement('div');switcher.className='learning-mode-switch';const groups={guide:cards.slice(0,4),compare:cards.slice(4,5),quiz:cards.slice(5,6)};const labels={guide:'解説',compare:'比較',quiz:'クイズ'};function show(mode){Object.values(groups).flat().forEach(c=>c.hidden=true);groups[mode].forEach(c=>c.hidden=false);[...switcher.children].forEach(b=>b.classList.toggle('is-selected',b.dataset.mode===mode))}Object.keys(groups).forEach(mode=>{const b=document.createElement('button');b.type='button';b.dataset.mode=mode;b.textContent=labels[mode];b.addEventListener('click',()=>show(mode));switcher.append(b)});grid.before(switcher);show('guide')}const toggle=document.createElement('button');toggle.type='button';toggle.className='library-toggle';toggle.textContent='リズム一覧';toggle.addEventListener('click',()=>library.classList.toggle('is-collapsed'));library.append(toggle);if(window.matchMedia('(max-width:520px)').matches)library.classList.add('is-collapsed');const sections=[picker,sequencer,learning,library];const buttons=[...nav.children];const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;buttons.forEach((b,i)=>b.classList.toggle('is-active',sections[i]===visible.target))},{rootMargin:'-20% 0px -65% 0px',threshold:[0,.2,.5]});sections.forEach(s=>observer.observe(s));})();